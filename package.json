{
  "name": "select-translate",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Chrome extension that translates selected text in a Google Translate-style popup.",
  "scripts": {
    "build": "node scripts/build.mjs bundle",
    "pack": "node scripts/build.mjs pack",
    "test": "node --experimental-strip-types --test scripts/test-shared.ts",
    "typecheck": "tsc --noEmit",
    "icons": "python3 scripts/generate_icons.py"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.24",
    "esbuild": "^0.25.10",
    "typescript": "^5.9.2"
  }
}
